name: date_time_package
description: A new Flutter package project.
version: 0.0.1+1
author:

environment:
  sdk: ">=2.8.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^1.2.0
  flutter_bloc: ^5.0.1
  flutter_modular: ^1.3.0
  intl: ^0.16.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
